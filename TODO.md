# TODO — Neon Parallax Starfield (Cyberpunk)

- [ ] Update `css/style.css` to add full-screen fixed starfield canvas styling (behind content, no pointer events, vignette).
- [ ] Update `js/app.js` to add a self-contained parallax starfield renderer (multiple depth layers, twinkling, particles/dust, smooth RAF loop, reduced-motion support).
- [ ] Add `<canvas id="starfield">` to `index.php`, `dashboard.php`, and `student-dashboard.html`.
- [ ] Add `<canvas id="starfield">` to remaining top-level pages (PHP/HTML entry pages) so it appears on every page.
- [ ] Smoke test by opening pages and verifying animation, performance, and no interaction blocking.
