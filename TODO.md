- [x] Inspected `sql/database.sql` and `sql/admin_database.sql`.
- [x] Removed the accidentally appended admin schema + seed data from `sql/database.sql` (kept only the main schema + seed data).
- [ ] (Next) Run the SQL in your MySQL/MariaDB client to confirm the original error is resolved.

